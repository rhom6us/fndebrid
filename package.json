{
  "name": "fndebrid",
  "version": "1.0.0",
  "private": true,
  "repository": "https://github.com/rhom6us/fndebrid.git",
  "author": "Thomas Butler <thomas@butler.software>",
  "license": "MIT",
  "main": "dist/main/main.js",
  "scripts": {
    "i": "yarn add",
    "reinstall": "yarn install --force",
    "typecheck": "lerna exec --parallel -- tsc --noemit",
    "watch:main": "yarn workspace @fndebrid/electron-main run compile",
    "watch:renderer": "yarn workspace @fndebrid/electron-renderer run compile",
    "watch:fnwebpack": "yarn workspace @fndebrid/webpack run compile:watch"
  },
  "build": {
    "files": [
      "dist/",
      "package.json"
    ]
  },
  "browserslist": [
    "electron 6.0.12",
    "current node"
  ],
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@types/*/**"
    ]
  },
  "dependencies": {
    "react-hot-loader": "^4.12.15"
  },
  "devDependencies": {

    "lerna": "^3.18.3"
    
  }
}
