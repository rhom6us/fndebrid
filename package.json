{
  "name": "fndebrid",
  "version": "1.0.0",
  "private": true,
  "repository": "https-//github.com/rhom6us/fndebrid.git",
  "author": "Thomas Butler <thomas@butler.software>",
  "license": "MIT",
  "scripts": {
    "i": "yarn add",
    "reinstall": "yarn install --force",
    "typecheck": "lerna exec --parallel -- tsc --noemit",
    "main-build": "yarn workspace @fndebrid/electron-main run compile",
    "main-watch": "yarn workspace @fndebrid/electron-main run watch",
    "renderer-build": "yarn workspace @fndebrid/electron-renderer run compile",
    "renderer-watch": "yarn workspace @fndebrid/electron-renderer run watch",
    "renderer-serve": "yarn workspace @fndebrid/electron-renderer run serve",
    "fnwebpack-watch": "yarn workspace @fndebrid/webpack run compile-watch"
  },
  "build": {
    "files": [
      "dist/",
      "package.json"
    ]
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/@types/*/**"
    ]
  },
  "dependencies": {
    "react-hot-loader": "^4.12.15"
  },
  "devDependencies": {

    "lerna": "^3.18.3"
    
  }
}
