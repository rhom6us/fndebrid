import { createAction,createReducer, ActionType } from "typesafe-actions";

import preferencesReducer from './reducer';
import * as preferencesActions from './actions';
export { preferencesActions, preferencesReducer };
